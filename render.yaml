# Render Blueprint for speak-it
# https://render.com/docs/blueprint-spec

services:
  # Backend API (Web Service)
  - type: web
    name: speak-it-api
    runtime: node
    region: singapore
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_TTS_API_KEY
        sync: false  # Set manually in Render dashboard
      - key: FRONTEND_URL
        sync: false  # Set manually: https://speak-it-xxxx.onrender.com

  # Frontend (Static Site)
  - type: web
    name: speak-it
    runtime: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
    envVars:
      - key: VITE_API_URL
        sync: false  # Set manually: https://speak-it-api-xxxx.onrender.com
